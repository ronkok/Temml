let defineFunction,mathMLTree,stretchy,mml;_ebe‍.w("../defineFunction",[["default",["defineFunction"],function(v){defineFunction=v}]]);_ebe‍.w("../mathMLTree",[["default",["mathMLTree"],function(v){mathMLTree=v}]]);_ebe‍.w("../stretchy",[["default",["stretchy"],function(v){stretchy=v}]]);_ebe‍.w("../buildMathML",[["*",null,function(v){mml=v}]]);




const mathmlBuilder = (group, style) => {
  const accentNode = stretchy.mathMLnode(group.label);
  return new mathMLTree.MathNode(group.isOver ? "mover" : "munder", [
    mml.buildGroup(group.base, style),
    accentNode
  ]);
};

// Horizontal stretchy braces
defineFunction({
  type: "horizBrace",
  names: ["\\overbrace", "\\underbrace"],
  props: {
    numArgs: 1
  },
  handler({ parser, funcName }, args) {
    return {
      type: "horizBrace",
      mode: parser.mode,
      label: funcName,
      isOver: /^\\over/.test(funcName),
      base: args[0]
    };
  },
  mathmlBuilder
});
